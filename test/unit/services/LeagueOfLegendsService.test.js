'use strict'
/* global describe, it */
const assert = require('assert')

describe('LeagueOfLegendsService', () => {
  it('should exist', () => {
    assert(global.app.api.services['LeagueOfLegendsService'])
  })
})
